version: 1
mcp_server:
  name: tailwind-svelte-assistant-mcp-server
  description: "Provides tools for Tailwind CSS and Svelte development, including class validation and documentation snippets."
  author: "Roo & User"
  version: "0.1.0"
startCommand:
  type: "stdio"
  configSchema:
    {}
  commandFunction: |-
    (config) => ({
      "command": "node",
      "args": [
        "dist/index.js"
      ],
      "env": {
      }
    })